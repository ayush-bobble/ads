pipeline {
    // ...

    stages {
        // ...

        stage('Print PATH') {
            steps {
                script {
                    // Print the current PATH
                    echo "Current PATH: ${env.PATH}"
                }
            }
        }

        // Add other stages as needed
    }

    // ...
}
